import {$} from "./common";


export default function isponitInPath(){
    const canvas = $("canvas");
    /** @type {CanvasRenderingContext2D} */
    const ctx = canvas.getContext("2d");

    // ctx.isPointInPath()

}